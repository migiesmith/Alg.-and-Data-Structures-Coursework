package com.grant.smith;

import java.util.Comparator;

class SavingsComparator implements Comparator<Customer> {
    @Override
    public int compare(Customer a, Customer b) {
        return a.name.compareToIgnoreCase(b.name);
    }
}